# Project

`opencode-beacon` is a Linux-first monitor for local OpenCode servers and Claude
Code CLI sessions.
The first release proves reliable discovery and event detection while keeping
notification policy out of scope.
